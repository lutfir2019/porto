{
  "name": "port",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "SET NODE_OPTIONS=--openssl-legacy-provider && nuxt",
    "build": "NODE_OPTIONS=--openssl-legacy-provider && nuxt build",
    "start": "NODE_OPTIONS=--openssl-legacy-provider && nuxt start",
    "generate": "NODE_OPTIONS=--openssl-legacy-provider && nuxt generate"
  },
  "dependencies": {
    "bootstrap": "^4.5.0",
    "bootstrap-vue": "^2.15.0",
    "nuxt": "^2.14.0",
    "vite": "^5.0.2",
    "vue-ionicons": "^3.0.5"
  },
  "description": "> Check it out [here](https://porto.vercel.app)",
  "main": "nuxt.config.js",
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "ISC"
}
